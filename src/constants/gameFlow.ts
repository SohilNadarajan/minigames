/** Host + controllers: show submitted guesses before the correct count / cube tally. */
export const GUESSES_DISPLAY_MS = 2500;

/** After a round’s reveal finishes, wait this long before starting the next round (host-driven). */
export const INTER_ROUND_DISCUSSION_S = 10;
